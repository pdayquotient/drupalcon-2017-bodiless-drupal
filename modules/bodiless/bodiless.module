<?php

/**
 * Implements hook_theme()
 */
 function bodiless_theme($existing, $type, $theme, $path) {
   return array(
     'bodiless' => array(
       'variables' => array('main_content' => NULL),
     ),
   );
 }
